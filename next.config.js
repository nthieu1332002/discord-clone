/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "uploadthing.com", "img.clerk.com", "utfs.io"
        ],
    }
}

module.exports = nextConfig
